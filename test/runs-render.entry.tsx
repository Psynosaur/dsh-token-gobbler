// test/runs-render.entry.tsx — bundling entry for the Runs-tab tests
// (test/runs-render.test.js): the per-model chart props the canvas engine is
// handed, including the per-home split when a model ran on more than one machine.
export { RunsTab } from "../client/runs";
export { setSourceIndex } from "../client/sources";
