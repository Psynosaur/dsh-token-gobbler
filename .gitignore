node_modules/
dist/
*.log
.npm-cache/
.DS_Store

# lib/*.js are GENERATED by `npm run build` (tsc from lib/*.ts, esbuild from client/*.ts).
# Commit the lib/*.ts sources; rebuild to produce the .js the DSH host loads.
lib/*.js
